@{
    // Every page sets its own layout explicitly; this is the fallback for one that forgets. Public
    // rather than _AppLayout on purpose — _AppLayout renders the signed-in chassis (team name, nav
    // badges), so a page that forgot to choose would otherwise render admin furniture around
    // whatever it is.
    Layout = "_PublicLayout";
}
