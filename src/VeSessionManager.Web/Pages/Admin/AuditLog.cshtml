@page
@model VeSessionManager.Web.Pages.Admin.AuditLogModel
@{
    ViewData["Title"] = "Audit Log";
    ViewData["ActiveNav"] = "AdminAuditLog";
    Layout = "_AppLayout";
}

<div class="page-head">
    <div>
        <p class="eyebrow">Most recent 200</p>
        <h1>Audit Log</h1>
    </div>
</div>

<div class="table-scroll">
<table class="cards" data-sortable="audit-entries">
    <thead>
        <tr>
            <th>When</th>
            <th>User</th>
            <th>Action</th>
            <th>Entity</th>
            <th>Details</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Entries.Count == 0)
        {
            <tr><td colspan="5">No audit entries yet.</td></tr>
        }
        @foreach (var entry in Model.Entries)
        {
            <tr>
                @* Sorts on the raw timestamp — "MMM d, yyyy" sorts alphabetically as text (Apr before Mar). *@
                <td class="mono" data-sort-value="@entry.TimestampUtc.ToString("o")">@EasternTimeFormatter.Format(entry.TimestampUtc, "MMM d, yyyy")</td>
                <td>@(entry.UserName ?? "(background job)")</td>
                <td>@entry.Action</td>
                <td class="mono">@entry.EntityType #@entry.EntityId</td>
                <td>@entry.Details</td>
            </tr>
        }
    </tbody>
</table>
</div>
