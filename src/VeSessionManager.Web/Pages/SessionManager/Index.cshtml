@page
@model VeSessionManager.Web.Pages.SessionManager.IndexModel
@{
    ViewData["Title"] = "Session Manager";
}

<h1>Session Manager</h1>
<p>You're logged in as <strong>@User.Identity?.Name</strong> (Session Manager).</p>
<p class="text-muted">Placeholder page — Phase 9a proves auth and role scoping work. The real candidate/session table lands in Phase 9b.</p>
