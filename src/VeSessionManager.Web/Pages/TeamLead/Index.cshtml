@page
@model VeSessionManager.Web.Pages.TeamLead.IndexModel
@{
    ViewData["Title"] = "Team Lead";
}

<h1>Team Lead</h1>
<p>You're logged in as <strong>@User.Identity?.Name</strong> (Team Lead).</p>
<p class="text-muted">Placeholder page — Phase 9a proves auth and role scoping work. Read-only session/candidate visibility (spec'd, backend-ready via <code>SessionAccessScope</code>, not yet wired to a page) is tracked as a known gap in TODO.md.</p>
