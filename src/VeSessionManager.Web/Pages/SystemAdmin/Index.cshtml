@page
@model VeSessionManager.Web.Pages.SystemAdmin.IndexModel
@{
    ViewData["Title"] = "System Administrator";
}

<h1>System Administrator</h1>
<p>You're logged in as <strong>@User.Identity?.Name</strong> (System Administrator).</p>
<p class="text-muted">Placeholder page — Phase 9a proves auth and role scoping work. Real system-wide provisioning (teams, Team Admin grants) lands in Phase 9c.</p>
