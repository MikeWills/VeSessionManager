{
  // Production overrides of the shared settings. Square goes live here; ExamTools' host is the same
  // in every environment, so it is not repeated.
  "Square": {
    "Environment": "Production"
  }
}
