info:
  name: Get Team's Sessions
  type: http
  seq: 1

http:
  method: GET
  url: https://exam.tools/api/veUser/sessions?team=HRCC
  params:
    - name: team
      value: HRCC
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
